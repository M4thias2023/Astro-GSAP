<section class="flex flex-col h-screen md:h-[calc(100dvh-85px)] md:flex-row justify-center">
    <main id="main" class="flex md:w-[400px] flex-col md:mt-0 md:ml-0 md:px-5 px-5 md:justify-center ">
      <div class="flex flex-col w-full md:w-auto">
        <div
          class="
            text-primary
            font-semibold
            leading-[97.5%]
            capitalize
            md:max-w-full 
            [font-size:clamp(55px,4vw,86px)]
          "
        >
          Dibujando
          <br />
          el camino
        </div>
        <div
          class="
            flex
            w-[253px]
            max-w-full
            items-start
            gap-2
            mt-1
            md:mt-3
            max-md:justify-start
          "
        >
          <div class="circle w-[40px] h-[40px] md:w-[50px] md:h-[50px] bg-red-500 rounded-full "></div>
          <div class="circle w-[40px] h-[40px] md:w-[50px] md:h-[50px] bg-blue-600 rounded-full"></div>
          <div class="circle w-[40px] h-[40px] md:w-[50px] md:h-[50px] bg-yellow-400 rounded-full"></div>
        </div>
        <div
          class="
          text-primary
            [text-wrap:balance]
            font-light
            leading-8
            md:leading-[38.24px]
            w-screen
            max-w-full
            mt-5
            md:mt-10
            md:text-4xl
            [font-size:clamp(23px,4vw,40px)]
          "
        >
          Recomendación de libros para desarrollar tu talento artístico
        </div>
      </div>
    </main>
    <div class="w-auto h-auto p-5 md:p-2 md:flex md:items-center md:w-[800px]">
      <img src="/paint.jpg" class="w-full h-auto main-img" alt="imagen principal">
    </div>
</section>

  <script>
    import {gsap} from 'gsap'

    const TimelineConfig = {
      defaults: {
        ease: "power1.out",
      }
    }

    let tl = gsap.timeline(TimelineConfig);
    let tl2 = gsap.timeline(TimelineConfig);

    function MainAnimation(){
      return gsap.fromTo('#main',{
        opacity:0,
        x: -500,
      },{
        opacity:1,
        x:0,
        duration:.5,
      })
    }

    function CirclesAnimation() {
      return gsap.fromTo('.circle',{
      'scale':'0',
      opacity:0,
      y:100,
    },{
      'scale':'1',
      opacity:1,
      y:0,
      stagger:0.2,
      duration:1,
    })
    }

    function ImageAnimation() {
      return gsap.fromTo('.main-img',{
        opacity:0,
        x: 500,
      },{
        opacity:1,
        x:0,
        duration:.5,
      })
    }

    tl.add(MainAnimation()).add(CirclesAnimation()).add(ImageAnimation())
    
  </script>
